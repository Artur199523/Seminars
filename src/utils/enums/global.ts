export enum NOTIFICATION {
   SUCCESS = "success",
   ERROR = "error",
   WARNING = "warning",
   INFO = "info",
}

export enum SIZE {
   SMALL = "small",
   MEDIUM = "medium",
   LARGE = "large",
}

export enum VIEW {
   DEFAULT = "default",
   PRIMARY = "primary",
   SECONDARY = "secondary",
}

export enum MODAL_CONTEXT_KEY {
   SEMINAR,
}

export enum SEMINAR_MODALS {
   CLOSE = "CLOSE",
   SEMINAR_DELETE = "SEMINAR_DELETE",
   SEMINAR_CREATE = "SEMINAR_CREATE",
   SEMINAR_UPDATE = "SEMINAR_UPDATE",
}
